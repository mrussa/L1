к каким негативным последствиям он может привести и как это исправить?
к утечке, data race, битой строке. создать новый буфер. strings.Clone новый буфер.

что происходит с переменной justString?
justString указывает на те же данные, что и v, с длиной 100. Пока justString жива, буфер v не может быть освобождён GC, что ведёт к удержанию памяти.